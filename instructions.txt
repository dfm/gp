Change all instances of PORT to your assigned port.

(local) ssh -L PORT:localhost:PORT lionxv.rcc.psu.edu

(remote) cd work
(remote) git clone https://github.com/dfm/gp
(remote) cd gp
(remote) qsub -I -l nodes=1:ppn=1 -l walltime=1:30:00 -q WHAT_QUEUE_DO_WE_USE

(job) module load python/2.7.3
(job) cd $PBS_O_WORKDIR
(job) ssh -f -N -R PORT:127.0.0.1:PORT lionxv.rcc.psu.edu
(job) ipython notebook --no-browser --port=PORT --matplotlib=inline

On your local machine, point open a web browser and point it at
http://localhost:PORT
